pipeline {
       agent any 
       stages {
               stage ("build stage") {
               echo "this is build stage"
               script {
                      2 + 3 == 5 ? true : false 
               }
               }
       }    
} 
